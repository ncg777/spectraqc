[project]
name = "spectraqc"
version = "0.1.0"
description = "Spectral QC tool (V1 CLI)"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.0",
  "pyloudnorm>=0.1.1",
]

[project.scripts]
spectraqc = "spectraqc.cli.main:main"
